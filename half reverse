#include<stdio.h>
int main() {
    int n=12345,ans=0,r=0;
    int c=floor(log10(n))+1;
    int half=(c/2)+1;
    int t_half=half;
    while(half)
    {
        r=n%10;
        ans=ans*10+r;
        half--;
        n=n/10;
    }
    int fans=n*pow(10,t_half)+ans;
    printf("%d",fans);
}
------------------------------------------------------------------------------------------------------------------
Array    
#include<stdio.h>
int main() {
    int n,L,r,temp;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
        scanf("%d",&a[i]);
    int half=n/2;
    L=half;
    r=n-1;
    while(L<r)
    {
        temp=a[L];
        a[L]=a[r];
        a[r]=temp;
        L++;
        r--;
    }
    for(int i=0;i<n;i++)
        printf("%d",a[i]);
}
