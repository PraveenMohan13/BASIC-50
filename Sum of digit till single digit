#include<stdio.h>
int main() {
    int n=8885,r,sum=0;
    while(n>9)
    {
        while(n!=0)
        {
            r=n%10;
            sum=sum+r;
            n=n/10;
        }
        n=sum;
        sum=0;
    }
    printf("%d",n);
}
